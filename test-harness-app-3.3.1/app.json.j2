{
  "name": "{{ agave.username }}-{{ app.name }}",
  "version": "{{ app.version }}",
  "executionType": "HPC",
  "executionSystem": "hpc-tacc-maverick2-{{ agave.username }}",
  "parallelism": "SERIAL",
  "deploymentPath": "{{ agave.username }}/apps/{{ app.bundle }}",
  "deploymentSystem": "data-sd2e-projects-users",
  "defaultProcessorsPerNode": 1,
  "defaultNodeCount": 1,
  "defaultQueue": "gtx",
  "label": "{{ date_time.iso8601_micro }}",
  "modules": [
    "load tacc-singularity"
  ],
  "shortDescription": "{{ app.description }}",
  "templatePath": "runner-template.sh",
  "testPath": "tester.sh",
  "inputs": [],
  "defaultMaxRunTime": "24:00:00",
  "parameters": [
    {
      "id": "CONTAINER_IMAGE",
      "value": {
        "default": "{{ docker.organization }}/{{ docker.repo }}:{{ docker.tag }}",
        "description": "Container Image. Do not edit.",
        "type": "string",
        "visible": false,
        "required": true
      }
    },
    {
      "id": "git_hash",
      "value": {
        "default": null,
        "description": "7 character git commit id pointing to the versioned data to be analyzed",
        "type": "string",
        "visible": true,
        "required": true
      }
    },
    {
      "id": "env",
      "value": {
        "default": "dev",
        "description": "Which env of Escalation to run the test harness for, from {dev, prd}",
        "type": "string",
        "visible": true,
        "required": true
      }
    }
  ],
  "outputs": []
}
